# PRD：体验测试问题修复（2026-09-17 测试批次）

| 项 | 内容 |
|---|---|
| 版本 | v1.1（范围核对通过、方案实证通过、D-1~D-4 已确认） |
| 日期 | 2026-09-18 |
| 需求来源 | 《APP体验问题与优化建议清单-20260917》《APP体验测试流程记录-20260917》（本 PRD 需求编号与清单 F/U/C/J 编号一一对应） |
| 被测基线 | Android APK versionCode 6 / versionName 1.1.4；价格数据版本 818c4593d4b15f51（通道 64 条 / 断面 10 个） |
| 涉及端 | Web 单文件应用（src/ 源码）→ 构建产物同步安卓 WebView 壳 |
| 交付纪律 | 原“仅存本地、不提交不推送”经用户于 2026-09-18 指示解除，随验证通过提交 PR；两份清单与本文档为临时文档，功能验收后删除 |

---

## 1. 背景

35 项体验测试全部执行完毕，无 S1 阻断项。上一批三项重点修复（底栏遮挡、天地图瓦片、容量电费测算器）均验证有效，容量电费手算核对与程序输出完全一致。

本批次新发现问题集中在三类：

1. **数值输入无校验**：超长数字打破主结果区与容量电费测算器版式（两处同源，U2/U3）；
2. **天地图密钥状态误报**：乱码密钥也显示"加载成功、自检正常"，实际瓦片未加载（F2）；
3. **新手引导缺失**：功能入口深、首次使用者难以独立完成完整测算（J2）。

## 2. 目标与非目标

**目标**

1. 主流程结果区对任意用户输入保持版式稳定，消灭破版；
2. 天地图密钥状态展示与事实一致，消灭假阳性（含空输入语义明确化）；
3. 新用户不看说明即可独立完成一次完整测算；
4. 顺带补齐两处低成本缺口：推荐失效提示（F4）、费率库容量电价分区（F3）。

**非目标**

- 不改任何算法与计价逻辑（`tools/baseline-check.mjs` 1739 项回归锁死）；
- 不引入任何第三方库/运行时依赖（保持单文件、无构建依赖原则）；
- 不做横屏重排版（U1 挂起，见 §5）；
- 不改安卓壳工程（F1 白屏属安卓侧改动，单独处理，见 §5）;
- 不更换底图供应商，不动底图合规白名单。

## 3. 需求总览

| 优先级 | 编号 | 需求 | 清单来源 | 一句话描述 |
|---|---|---|---|---|
| P0 | FR-1 | 数值输入校验与长数字缩略显示 | U2/U3/J1 | 输入 clamp + 显示缩略 + CSS 兜底，三层防御消灭破版 |
| P0 | FR-2 | 天地图密钥真实校验与空输入语义 | F2/J5 | 瓦片级探针验证密钥；空输入不再半清空状态 |
| P0 | FR-3 | 新手引导与帮助入口 | J2 | 首启导览卡片 + 常驻帮助入口 + 测算器入口提示 |
| P1 | FR-4 | 智能推荐失效提示 | F4 | 参数变化后旧推荐原位留灰字说明 |
| P1 | FR-5 | 费率库容量电价分区 | F3/J4 | 费率库增设第 4 个分区，29 省分档单价可独立查验 |
| P2 | — | 挂起项 | U1/U4/C1/C2/J3/U5/F1 | 见 §5 |

## 4. 需求明细

### FR-1 数值输入校验与长数字缩略显示（U2/U3/J1）

**现状与事实依据**

- 输入侧无上限：主输入 `state.pGen=+g('i-pgen').value||0`（src/app/ui/calc.js:823）；容量电费测算器 `parseFloat` 后仅拦负数（src/app/boot.js:46-47）。`type="number"` 接受任意长度数字串。
- 显示侧无量级处理：`num()` 千分位分组产生不可断行长字符串（src/app/format.js:2）。
- CSS 侧无防御：`.hero-v` 34px 大字无断行/溢出规则（src/template.html:256）；`.g3` 三列网格子项无 `min-width:0`（:213）；`.mc .v` 无溢出处理（:207）。长数字把网格轨道撑开，将"度电分摊"卡推出屏幕右缘。
- 异常值经 `saveLast()` 写入 localStorage，重启后持续复现。

**需求描述（三层防御）**

1. **输入层**：新增纯函数 `sanNum(v,{min,max,fallback})`（放 src/app/format.js，可被模块测试覆盖）。所有数值输入接入业务上限：送端报价/各类费率 ≤ 10,000 元/MWh；省间交付电量 ≤ 10,000,000 MWh；时长 ≤ 8,760 h；容量/需量 ≤ 1,000,000 kVA(kW)；年用电量 ≤ 10,000,000 MWh；百分比类维持现有 min/max。超限时：state 只存 clamp 后的值，输入框红框 + 输入框下方红字提示（复用现有 `.warn` 样式），提示在输入合法值后消失。
2. **显示层**：新增 `fmtCompact(v)`：|v| ≥ 1e8 显示"X.XX亿"、≥ 1e4 显示"X.X万"、其余维持现状；完整数值放 `title` 悬浮。仅用于 hero 大字（`.hero-v`）与容量电费三卡（`.mc .v`）等展示位；**表格内精组数字保持原样**（保护数据可信感，见清单"整体印象 1"）。
3. **CSS 兜底**：`.hero-v{overflow-wrap:anywhere}`；`.g3>*{min-width:0}`；`.mc .v{overflow:hidden;text-overflow:ellipsis;white-space:nowrap}`。保证未来任何异常值都不再破版。

**验收标准**

- AC1：送端报价输入 20 位以上数字，主结果区（落地成本卡片、费用组成条、送端净收益）版式正常，数值以万/亿缩略显示，悬浮可见完整值；
- AC2：容量电费测算器容量/电量输入超大数，三卡布局正常，无卡片被推出屏幕；
- AC3：超限输入触发红框+红字提示，修正后消失；杀进程重开后输入为 clamp 后合法值；
- AC4：`tools/test-modules.mjs` 新增 sanNum/fmtCompact 单测通过；`tools/baseline-check.mjs` 1739/1739 通过。

### FR-2 天地图密钥真实校验与空输入语义（F2/J5）

**现状与事实依据**

- 假阳性机制：`tdTileProbe` 用 `fetch(url,{mode:'no-cors'})`，对任何 HTTP 状态（含 4xx/5xx）都 resolve，只能证明"传输可达"，证明不了密钥有效（src/app/ui/map.js:399-413，注释自认）。乱码密钥 → SDK 类注册成功 → 绿字"加载成功" + 自检"传输正常"，实际瓦片全灭。
- 空输入半清空：`applyTk` 清空 `state.tiandituKey`（map.js:379），但 `loadTianditu` 中 `tdApiReady()` 排在空密钥检查之前（map.js:119-120）——SDK 已加载时 `T` 对象常驻，短路返回成功，地图继续可用且绿字覆盖红字（"红字一闪"）。重启后才走空密钥降级分支，行为前后不一致。属 bug，非设计。
- 外部约束：api 端点在真机环境对无效 tk 仍下发 SDK 脚本（真机实测），密钥有效性只能在瓦片端点验证；浏览器平台拿不到跨域响应状态码（天地图无 CORS 头）。

**需求描述**

1. **空输入语义**：`applyTk` 在输入为空时**不改动** `state.tiandituKey`，显示常驻灰字"输入为空，已保留本机已存密钥（尾号××××）"；新增显式"清除密钥"按钮，点击后清空存储并立即降级为内置拓扑图。
2. **瓦片级真校验**：`tkMsg('ok')` 改为过渡态"SDK 已加载，正在验证瓦片…"；`tdTileProbe` 改用 `new Image()` 加载一张固定 vec_w 瓦片（与 SDK 实际瓦片同域同格式）：
   - onload 且 `naturalWidth ≥ 256` → 绿字"密钥有效，底图可用"；
   - onerror → 常驻红字"瓦片请求被拒（密钥无效或被风控拦截）"。
   - 原理：无论服务端以 403 还是拦截页（HTML）拒绝，均不可解码为图片，onerror 必然触发，与拒绝方式无关，绕开状态码不可得的平台限制。
3. **失败处置**：探针失败时保留地图容器与叠加物 + 常驻红字，不强制降级（避免视图抖动）；文案区分"密钥无效 / 网络不可达 / 风控拦截"三种定性（复用 `tkFailShow` 二分思路）。

**验收标准**

- AC1：乱码密钥 → 不再出现"加载成功"绿字，显示常驻红字，底图不可用事实与提示一致；
- AC2：正确密钥 → 过渡态后显示"密钥有效，底图可用"，瓦片正常；
- AC3：删空输入点"应用" → 密钥保留、地图不中断、显示"已保留"常驻灰字；点"清除密钥" → 立即降级拓扑图，重启后不回弹；
- AC4：断网态 → 提示网络不可达，不误报"密钥无效"；
- AC5：状态机分支在 e2e 中以 mock Image 断言通过。

### FR-3 新手引导与帮助入口（J2）

**现状与事实依据**：全库无任何引导/帮助/首次运行代码；容量电费测算器为折叠 `<details>`（calc.js:924），组件筛选、费率库筛选等入口深。本地存储键命名惯例为 `iproute.v2.*`（src/app/config.js:4-5）。

**需求描述（轻量实现，不引第三方 tour 库）**

1. 首启导览：新增存储键 `iproute.v2.guide`；**首次启动 = 安装后本机第一次运行**（判定：该存储键不存在），而非每次结束进程后的重开。首次弹出 3~4 步说明卡片（复用现有 sheet 面板样式）：三个 Tab 各是什么 → 最短测算路径（选起止省即出结果）→ 容量电费测算器入口 → 天地图需自备 tk。**任意步骤均可一键「跳过」**（按钮显著，首屏即有），跳过与走完同等写入存储、此后不再弹出——键随 localStorage 持久化（同 `setDomStorageEnabled(true)`，即测试项 8.2"杀进程重开输入保留"的同一机制），结束进程重开不重置；
2. 页头常驻"帮助"入口，随时重看导览；
3. 容量电费测算器 summary 增加一句灰字说明（如"算一笔容量电费的独立小工具"），降低被忽略概率。

**验收标准**

- AC1：安装后第一次运行弹导览，任意步骤可一键「跳过」；走完或跳过均写入存储；**结束进程重开不再弹**；仅清除应用数据/存储键后重置；
- AC2：清除该存储键后重开再次弹出；帮助入口任意时刻可用；
- AC3：导览不阻塞测算主流程（可随时关闭，关闭后页面可直接操作）。

### FR-4 智能推荐失效提示（F4）

**现状与事实依据**：`renderAI` 已有过期判断 `raw.res!==res`（src/app/ui/ai.js:148），命中时旧结果被静默丢弃，无任何提示。

**需求描述**：过期分支不再静默丢弃，改为原位灰色提示块："参数已变化，原推荐已失效"+「重新推荐」按钮（点击即用当前参数重跑 `aiRun`）。错误与加载态行为不变。

**验收标准**：跑出推荐后修改受端省份，原推荐位置显示失效提示而非消失；点击「重新推荐」按新参数发起请求。

### FR-5 费率库容量电价分区（F3/J4）

**现状与事实依据**：数据 `DATA.CAP`（29 省分电压档容量/需量单价 + 来源字段）已在共享数据内（src/app/data.js:5），测算器已在用（calc.js:922）；费率库仅 通道/省级参数/断面 三个 tab（src/app/ui/lib.js:107-109）。

**需求描述**：

1. 费率库增设第 4 个分区"容量/需量电价"，表格列：省份 / 档别 / 容量电价 / 需量电价 / 来源；接入现有搜索与 tier 筛选；
2. 测算器"所选档单价"卡片增加本省全部电压档展开（J4），展示各档单价与档别名。

**验收标准**：费率库可独立查到任一省任一档单价及其来源，与测算器取值一致；搜索"北京"可在该分区命中。

## 5. 挂起项（P2）与归因结论

| 项 | 归因结论（代码/历史证据） | 处置 |
|---|---|---|
| U1 横屏留白 | 现仅 `@media (min-width:600px)` 调整 sheet 面板（template.html:305），无横屏布局 | 挂起；需单独设计三页横屏两栏方案 |
| U4 天地图无站名标注 | **有意移除，非缺失**：commit 028e0dc（2026-09-17）"标注文字多余——移除 T.Label"，同时移除了省名（`N(k)`）与站点名（`p.n`）。省名因天地图 vec 瓦片自带地名文字，移除成立；站点名为电力设施，底图瓦片**没有**替代信息 | 省名维持移除；**站点名是否恢复"仅选中路线"标注列为待决策 D-1**（原实现即只标选中方案站点，恢复成本低） |
| C1 组件命中视觉态 / C2 卡片显示全部途经段 | 交互增强 | 挂起；C2 可先在卡片加一行途经段名 |
| J3 拓扑图点选切换方案 | 交互增强 | 挂起 |
| U5 拖动中路线消失 | 天地图 SDK 矢量叠加物在惯性拖动期间的重绘行为，应用侧无 hook | 挂起；性价比低 |
| F1 启动白屏（含 17s 异常） | 壳工程零加载反馈：MainActivity.java（48 行）无 splash/onPageFinished/进度指示。2 秒白屏=WebView 正常首绘；17s 异常无法静态归因 | 挂起；安卓侧单独改动（windowBackground 底色 + onPageFinished 渐显），17s 异常需真机 logcat 复测 |

## 6. 约束与非功能需求

1. 算法层（src/app/algo/*）保持纯函数，不引入界面全局（test-modules.mjs 模块守卫①）；新工具函数放 format.js；
2. 代码中不内嵌任何有效地图密钥；`__WB_HTTP_PORT__` / `__WB_TMAP_SECRET__` 占位符原样保留；底图仅腾讯/天地图白名单；
3. 本地存储键沿用 `iproute.v2.*` 命名；
4. 构建一次产出三份同源产物（index.html / shared/app-data.json / min），`priceVersion` 与 `dataHash` 不因本次改动变化（未改数据文件）。

## 7. 验收与验证计划

**自动化**（每次改动后全绿）：

```bash
node tools/build.mjs             # 重建 index.html
node tools/baseline-check.mjs    # 1739/1739 必须全通过
node tools/test-modules.mjs      # 模块守卫 + 新增 sanNum/fmtCompact 单测
node tools/test-data-share.mjs   # 三端产物一致性
```

**手工**：FR-1 三处超长输入；FR-2 四态（正确 tk/乱码 tk/空输入/断网）；FR-3 首启/二次启动/帮助入口；推荐失效提示；费率库新分区。真机验收用 `node android/build-apk.mjs` 出本地 debug 包（构建→拷 assets→gradle）。

**交付纪律**：所有改动仅存本地；两份测试清单与本 PRD 保持 git 未跟踪；不 commit 不 push，验收通过后另行决定。

## 8. 决策记录（已确认，2026-09-18）

| # | 决策 | 结论 |
|---|---|---|
| D-1 | U4：站点名标注是否恢复 | **不恢复，维持现状**。省名/站点名均为 028e0dc 有意移除（"标注文字多余"），U4 关闭为"符合设计"。如后续需要，恢复"仅选中路线站点"文字标注成本低，另行立项 |
| D-2 | FR-2：瓦片探针失败后是否强制降级 | **不降级**，保留地图容器与叠加物 + 常驻红字 |
| D-3 | FR-1：输入业务上限数值 | **按 FR-1 需求描述采纳**（报价/费率 ≤10,000 元/MWh、电量 ≤1e7 MWh、时长 ≤8,760 h、容量 ≤1e6 kVA(kW)、年用电量 ≤1e7 MWh）；实现集中为常量表，后续调参不动逻辑 |
| D-4 | FR-3：导览触发时机 | **安装后本机第一次运行即弹**（存储键判定，非每次进程启动），配合任意步骤可「跳过」 |

## 9. 验证记录（2026-09-18，方案定稿前实证）

以下均为本机真实执行结果（非推断），环境：http://127.0.0.1:8123/index.html（当前 main 构建产物），Playwright headless msedge，视口 390×844。

| # | 验证内容 | 方法 | 结果 |
|---|---|---|---|
| V0 | PRD 覆盖清单完整性 | 脚本对清单 16 个编号（F1-F4/U1-U5/C1-C2/J1-J5）逐项与 PRD 对照 | **16/16 全覆盖**（含挂起项 §5） |
| V1 | U2 破版复现 | 真实浏览器输入 20 位送端报价 | hero-v 文本"32405156513821294592.00元/MWh"，内容宽 **412px > 盒宽 304px**（视口 390px），溢出坐实 |
| V2 | U3 破版复现 | 容量电费测算器输入超大数 | 三卡右缘 **[104, 353, 560]**，第三张"度电分摊"卡被推出视口 **170px**；g3 内容宽 529/盒宽 328——与测试记录"把度电分摊卡推出屏幕右侧"逐字吻合 |
| V3 | FR-1 CSS 兜底有效性 | 注入 §4 FR-1 第 3 条三条 CSS 后复测同场景 | hero 304/304、g3 328/328、三卡右缘 [135, 247, 359] 全部入视口，**破版消除** |
| V4 | FR-2 探针机制 | 同一乱码 tk 瓦片 URL 双通道实测 | `fetch(no-cors)`=**resolved**（现行自检"传输正常"假阳性机制实锤）；`Image`=**onerror**（修复方案可真实识别拒绝） |
| V5 | FR-5 数据在场 | node 解析 shared/app-data.json | CAP **32 省 / 240 档位条目 / 30 省带来源字段**，分区可行 |

**证据边界（如实声明）**：

1. V4 证明的是"请求被拒 → Image onerror 必然触发"这一修复机制；"乱码密钥在真机被拒"由测试者真机证据（瓦片灰底+裂图，即图片加载失败）与本次 V4 互为印证。**"有效密钥 → 瓦片 onload"正向分支无法在本机验证**——本机网络路径被天地图 CloudWAF 拦截（curl 矩阵实测：乱码 tk/无 tk 均返回 418 拦截页，与密钥无关），该分支依赖标准 img 加载行为与测试记录 5.5（正确密钥瓦片加载通过）兜底，实施后按 §7 真机四态复验。
2. V1-V3 的修复验证仅覆盖 CSS 兜底层；输入 clamp 与显示缩略两层属实施后验证项（test-modules 单测 + e2e，见 FR-1 AC4）。

---

*归因证据均来自当前 main 工作区源码、git 历史（提交 028e0dc 等）与 §9 实证记录；D-1~D-4 已于 2026-09-18 确认，无遗留待定项。*
