# 执行报告：体验问题修复批（FR-1~FR-5）

| 项 | 内容 |
|---|---|
| 任务 | 《任务-按PRD实施FR-1至FR-5-体验问题修复批-20260918.md》 |
| PRD | 《PRD-体验问题修复-20260918.md》（v1.1） |
| 执行日期 | 2026-09-18 |
| 交付纪律 | 全部改动仅存本地工作区，**未 commit、未 push、未动远端**；PRD、两份测试清单及本报告均为 git 未跟踪文件 |

---

## 1. 基线（开工前复核确认）

- **HEAD：`8466155753615c1797a6a87791e68743cb7e4ba2`**（`git log -1 --format="%H %s"`：Merge branch 'main' of github.com:ChenShi-Tech/interprovince-route-optimizer）
- 本地 main = origin/main（`git status` 无 ahead/behind）；工作区干净（仅 3 个约定未跟踪 md）
- **priceVersion `5f5f316cff2c0bb4` / dataHash `312542903d5ab56e…`**；通道 91 条（86 带价）/ 断面 10 个 / 站点 102 / 省级参数 31
- 上述口径按用户《基线确认补充 20260918》第 1 条执行，**替代 PRD 第 8 行的 818c4593/64 通道**；施工期间未执行任何 fetch/pull

### 第 0 步过程记录（如实）
1. 首次 `git fetch origin` 因本机网络访问 github.com 超时失败（300s timeout）；当时工作区有 3 个已跟踪文件改动（上一批 REQ-401 未提交成果）、本地 main 落后 origin/main 53 提交。按提示词 P2 停止并报告，未做任何修复动作。
2. 经只读 `git diff` 查明：PRD 既定基线（818c4593）即该未提交工作区；请示后用户未选择，期间**用户自行完成** `git checkout -- 三个脏文件 + git pull --ff-only` 并下发《基线确认补充》。
3. 按补充指示重跑第 0 步：三项全部满足后开工。补充指示第 2 条要求记录 HEAD 完整哈希（即本节）；施工期间除只读 `git status/log/diff` 外无任何 git 动作（超出原提示词 H6 五条命令范围的均为用户补充指示所要求或只读核对）。

---

## 2. 改动清单（逐文件）

| 文件 | 改动内容 | 对应 FR | 对应 PRD 条目 |
|---|---|---|---|
| src/app/format.js | 新增纯函数 `sanNum(v,{min,max,fallback})`（非数回退 fallback、超限夹取）；新增 `fmtCompact(v,d)`（≥1e8→X.XX亿、≥1e4→X.X万、其余维持现状：d=0 等同 num() 千分位、d>0 等同 fmt(v,d)） | FR-1 | §4 FR-1 需求描述 1、2；§8 D-3 |
| src/app/ui/calc.js | ①新增 `NUM_LIMITS` 上限常量表（报价/输配电价/基金/系统运行费 ≤10000，电量/年用电量 ≤1e7，时长 ≤8760，容量 ≤1e6）与 `_numWarn/_numClamped/_numBad/_numWarnHtml/_numVal` 输入层防御；②`readInputs` 接入 clamp（i-qty/i-hours/i-pgen/i-pnet/i-fund/i-dstsysop，回落语义维持现状）；③hero 报价框、bar-params 两框、参数面板三框接入红框+红字+原始值回显；④`renderHeroResult` hero 大字改 `fmtCompact(r.landed,2)` 并挂 `title` 完整数值；⑤`renderCapFee` 三卡改 fmtCompact 并挂 title；⑥summary 增加灰字「算一笔容量电费的独立小工具，不参与路径比选」；⑦卡内新增「本省全部电压档单价」折叠表（档别×容量/需量单价，标注当前档） | FR-1、FR-3③、FR-5② | §4 FR-1、FR-3.3、FR-5.2 |
| src/app/boot.js | ①REQ-401 分支 i-capval/i-capqty 接入 `_numClamped`；②启动末尾调用 `maybeGuide()` | FR-1、FR-3 | §4 FR-1.1、FR-3.1 |
| src/app/ui/map.js | ①`applyTk` 空输入早退：不改 `state.tiandituKey`、不重渲染、不打断地图，`tkMsg('keep')` 常驻灰字「输入为空，已保留本机已存密钥（尾号××××）」；②新增 `clearTk()`（清空存储并立即降级内置拓扑图）；③td 面板新增「清除密钥」按钮；④`tkMsg('ok')` 改过渡态「SDK 已加载，正在验证瓦片…」；⑤`tdTileProbe` 由 `fetch(no-cors)` 改为 `new Image()` 加载固定 vec_w 瓦片：onload 且 naturalWidth≥256→绿字「密钥有效，底图可用」；onerror→三分法定性（`tdTileFail`/`tdTileFailShow`：fetch 探 api 成功=「瓦片请求被拒（密钥无效或被风控拦截）」并置 10 分钟冷却；fetch 拒绝=「网络不可达」，不误报密钥无效）；⑥探针失败不强制降级（保留容器与叠加物，D-2），删除原「瓦片自检：传输正常」假阳性文案 | FR-2 | §4 FR-2.1/2/3；§8 D-2 |
| src/app/config.js | 新增存储键 `LS_GUIDE='iproute.v2.guide'`（沿用 iproute.v2.* 命名） | FR-3 | §6.3 |
| src/app/state.js | 新增首启导览：`GUIDE_STEPS`（4 步：三 Tab 介绍→最短测算路径→容量电费测算器入口→天地图需自备 tk）、`openGuide/closeGuide/guideNext/renderGuide/guideDone/maybeGuide`；复用 sheet 面板样式；任意步骤「跳过」/走完/点遮罩/X 均写入存储键（保守口径，见 §5 待裁决-3）；代码对 vm 测试桩做防御性守卫（与既有 snapDetails 同风格） | FR-3 | §4 FR-3.1/2 |
| src/app/ui/ai.js | `renderAI` 过期分支不再静默丢弃：原位灰字提示块「参数已变化，原推荐已失效。」+「重新推荐」按钮（onclick=aiRun，按当前参数重跑）；错误与加载态行为不变 | FR-4 | §4 FR-4 |
| src/app/ui/lib.js | 费率库新增第 4 分区「容量/需量电价」：tab 按钮（含主体数角标）、搜索框（`capSearch`，匹配省/键/档别/来源）、档别筛选（全部 distinct 档别 seg 按钮，`capTierF`）、五列表格（省份/主体、档别、容量电价、需量电价、来源-省略+title）；`capLibRows/capLibTiers/renderCapList` 与 DATA.CAP 同源，含冀北 HE_B、深圳 GD_SZ 两非省级主体 | FR-5 | §4 FR-5.1 |
| src/template.html | CSS 兜底：`.hero-v{overflow-wrap:anywhere}`、`.g3>*{min-width:0}`、`.mc .v` 溢出省略三件套；`input.num-bad` 红框样式；`.ai-stale` 灰字块；导览步进样式（.guide-*）；页头新增常驻「帮助」按钮；新增 `#guide-sheet` 导览面板壳（复用 .sheet 样式） | FR-1、FR-3、FR-4 | §4 FR-1.3、FR-3.2 |
| tools/test-modules.mjs | 新增「五、format.js 纯函数单测」：vm 加载 format.js，sanNum 8 项 + fmtCompact 8 项断言；新增 `import vm`（APP_FILES 清单未动，与 build.mjs 一致性校验不受影响） | FR-1（AC4） | §4 FR-1 AC4 |
| tests/e2e.mjs | 新增 11 条用例（X1-01/02/03、X2-01~05、X3-01/02、X4-01、X5-01/02）；`runTest` 为非导览用例预置 `iproute.v2.guide` 存储键（FR-3 导览按设计全屏遮挡，专例 freshGuide 自清）；E-02 期望按 FR-2/D-2 翻转（无效密钥→常驻红字+不降级，替代旧「降级清单」）；RQ-401 三卡显示断言按 FR-1 缩略改校 title 完整数值（手算语义不变），其 summary 定位器改 `#d-capfee > summary`（卡内新增嵌套 details 致原选择器命中 2 元素） | 全 FR 的 e2e 覆盖 | §7；H8 |
| index.html / shared/app-data.json / shared/app-data.min.json | 构建产物重建（`node tools/build.mjs`）。**与 HEAD 版 diff 仅 `builtAt`/`BUILD_TIME`，priceVersion 与 dataHash 未变化**（见 §3 门禁-1 后校验） | — | §6.4（H5） |

**零改动声明**：`src/app/algo/*` 未动（保持纯函数，baseline-check 987/987 等价性通过）；未引入任何第三方库/运行时依赖；未内嵌任何有效地图密钥；`__WB_HTTP_PORT__`/`__WB_TMAP_SECRET__` 占位符原样；底图白名单未动；挂起项 U1/U4/C1/C2/J3/U5/F1 一律未动（C2「途经段名」未顺手实现）。

---

## 3. 门禁原始输出摘要（T2）

> 本机默认 Node 为 v18.20.5，playwright-core 1.63 要求 Node ≥20：e2e 使用临时下载的 Node v20.18.1 portable（`%TEMP%\node20portable`，不动仓库）执行；其余门禁 Node 18 可跑。e2e 前先 `node tests/dev-server.mjs`（:8734）。

**① `node tools/build.mjs`** — 通过（exit 0）：
```
通道总数: 91（专项工程 46 + 省间联络线 45）… 分档合计 91 = 通道总数
站点: 102 | 断面: 10 | 省级参数: 31 （全部有值）
应用模块: 13 个（167.8 KB）
价格数据指纹 priceVersion: 5f5f316cff2c0bb4
index.html 已生成: 523.5 KB / shared/app-data.json 已生成: 346.5 KB
```
构建后校验：`shared/app-data.json` 的 `git diff` 仅 `builtAt` 一行（05:05:06.333Z→06:01:04.408Z），**priceVersion `5f5f316cff2c0bb4`、dataHash `312542903d5ab56e` 与基线一致（H5 满足）**。

**② `node tools/baseline-check.mjs`** — **987/987 通过**（exit 0）：
```
基线：425 个省对，987 条路径
费率快照 sha256 abc70427b736d62673d8…（已还原，排除费率差异）
校验对象：index.html 内嵌算法
结果：987/987 通过
✅ 全部一致，实现与基线等价。
```
⚠️ 任务提示词要求「必须 1739/1739」，**1739 为旧基线（818c4593/64 通道时代）的用例条数**；按《基线确认补充》以新基线为准，当前基线条数为 987，**全量通过、0 不一致**（算法层零改动）。此偏离本报告如实记录。

**③ `node tools/test-modules.mjs`** — **46/46 通过**（exit 0，原 30 项 + 新增 sanNum/fmtCompact 单测 16 项）：
```
══ 五、format.js 纯函数单测（FR-1：sanNum / fmtCompact）══
  ✅ sanNum / fmtCompact 已定义并导出（src/app/format.js）
  ✅ sanNum 超上限夹到 max（报价 20000→10000）／低于下限夹到 min／非数值回退 fallback／空输入回退默认 0
  ✅ sanNum undefined 回退 fallback（8760h 上限场景）／区间内不改动／电量 1e8→1e7／容量 2e6→1e6
  ✅ fmtCompact ≥1e8 显示亿／亿保留两位小数／≥1e4 显示万／<1e4 维持 toFixed(d)
  ✅ fmtCompact d=0 维持 num 千分位／空值显示 —／负值按绝对值缩略
✅ 结果：46 项通过，0 项失败
```

**④ `node tools/test-data-share.mjs`** — **46/46 通过**（exit 0）：三端产物一致性、priceVersion/dataHash 同口径校验全部一致（末行「结果：46 项通过，0 项失败」）。

**⑤ e2e**（设施：tests/dev-server.mjs + tests/e2e.mjs，Playwright headless msedge，390×844；命令 `node tests/dev-server.mjs` 后 `BROWSER=msedge node tests/e2e.mjs`，本次用 Node20 portable）— **70/70 通过**（3 个未实现用例跳过，不计入通过率；exit 0）：
```
完成：70/70 通过（跳过 3 个未实现用例，不计入通过率）；报告 tests/report.md
```
本批新增用例全部通过：X1-01/02/03（FR-1 三处超长输入：clamp、红框红字、万/亿缩略、三卡右缘 [实测] 入视口、修正后提示消失）、X2-01~05（FR-2 mock Image 断言状态机：onload(256)→绿字；onerror+fetch resolve→「瓦片请求被拒」且不降级；onerror+fetch reject→「网络不可达」；空输入保留+灰字；清除密钥降级+重启不回弹）、X3-01/02（FR-3 首启/跳过/二启/走完/帮助入口/清键重置）、X4-01（FR-4 失效块+重新推荐）、X5-01/02（FR-5 分区搜索/档别筛选/全档展开/灰字）。既有用例无回归（F/I/B/E/HR/R/RQ 全绿）。
**H8 证据边界**：FR-2 正向分支（有效密钥→瓦片 onload）仅经 mock Image 断言，本机网络下未真实验证（天地图 CloudWAF 418，PRD §9 已声明）；报告中无任何「正向分支已真实验证」表述。

## 4. T3 本地 debug 包（环境原因失败，按提示词跳过）

```
node android/build-apk.mjs
→ Error: ENOENT: no such file or directory, scandir 'C:\Users\21676\android-toolchain'
  at file:///…/android/build-apk.mjs:14:19 （依赖 ~/android-toolchain 便携 JDK 17 + Gradle 8.7）
```
本机缺安卓工具链，按任务 T3 约定如实报告失败输出并跳过，**不计任务失败**。（脚本在加载期即失败，未触发其中 `node tools/build.mjs` 子步骤，构建产物仍为 §3-① 那次。）

---

## 5. 待裁决项（PRD 未写死，均按最保守方案实现并已显著标注）

1. **FR-1 超限输入框内显示值（H7 示例项）**：PRD 只说「state 只存 clamp 后的值」未写框内显示原始值还是 clamp 值。按最保守口径实现为**框内保留用户原始值 + 红框 + 红字提示**（若显示 clamp 值，则重渲染后红框红字立即消失，与 AC3「超限触发红框+红字提示，修正后消失」矛盾）。重启后框内为 clamp 合法值（瞬态标记不持久化）。
2. **FR-5「接入现有搜索与 tier 筛选」的 tier 含义**：现有通道分区的 tier 指可信度档（gov/grid/region/est），CAP 数据无该字段；J4 上下文为电压档。按最保守且可落地口径实现为**电压档（档别）筛选**（全部 distinct 档别按钮组）+ 搜索框。若需按可信度档筛选，需另定映射规则。
3. **FR-3 导览关闭路径的存储写入**：PRD 只明确「跳过与走完同等写入」。按最保守口径，**点遮罩/X 关闭也写入**（任何主动关闭都视为「已看过」，避免下次启动重复打扰）；如需「关闭不写键、下次再弹」，改 `closeGuide` 一处即可。
4. **E-02/RQ-401 两条既有 e2e 期望的翻转**（非删断言）：E-02 旧期望「无效密钥→降级清单」与 FR-2 机制（SDK 对无效 tk 仍 200 下发）+ D-2（不强制降级）直接冲突，按 PRD 新行为改写为「常驻红字+容器保留+无假阳性绿字」；RQ-401 手算值断言改经 `title` 完整数值校验（显示文本按 FR-1 缩略），数值语义一条未减。
5. **baseline-check 用例总数**：提示词/PRD 正文「1739」为旧基线条数，新基线为 987（用户基线确认第 1 条），全量通过。如后续数据再扩库，条数以 regression-baseline 实际为准。
6. **FR-4 界面可达性**：`AI_ENABLED=false`（2026-09-17 决策，属挂起外现状），失效块在界面不可达，本批不翻转该开关（H1 范围冻结）；逻辑层经 e2e X4-01 直接断言 `renderAI` 输出与 `aiRun` 重跑路径。界面验收需临时改回 true 另行构建（见手工验证清单 FR-4 节）。

---

## 6. DATA.CAP 实测口径（H9）

`node` 解析 `shared/app-data.json`（priceVersion 5f5f316cff2c0bb4）实测：

| 口径 | PRD 正文 | PRD §9 V5 | 用户基线确认 | **实测** |
|---|---|---|---|---|
| 主体条数 | 29 省 | 32 省 | 35 省 | **33**（31 个省级键含海南/西藏 ＋ 冀北 HE_B ＋ 深圳 GD_SZ） |
| 档位条目 | — | 240 | — | **256**（需量 128 ＋ 容量 128） |
| 带来源 | — | 30 省 | — | **33/33（全部带来源）** |

- 西藏（XZ）已补录：1 档、带来源（西藏自治区发改委 2026-07-31 通知附件），PRD 中「西藏暂无数据」的表述已过时；测算器「暂无数据」分支现为运行时防御（e2e RQ-401 以临时删除条目守住该分支）。
- 冀北、深圳为电网主体级键，不在 PV 省份选择器内：测算器省份下拉不含二者，费率库新分区如实列出。
- distinct 档别 12 种（1~10（20）千伏、35千伏、110千伏、220千伏及以上、35~110千伏、不满1千伏、110（66）千伏、1~10千伏、10（20）千伏及以下、101至3000千伏安、3001千伏安及以上、不分电压档）。
- **结论：以实测 33 主体 / 256 档 / 33 带来源 为准**，PRD 各口径与用户确认口径的不一致均记录于此。

---

## 7. 结论

FR-1~FR-5 全部实施完成，各自 AC 经单测/e2e 覆盖并通过；算法与计价逻辑零改动（987/987 等价）；H5 数据指纹不变；H8 证据边界如实声明；挂起项未动。自动化门禁：build ✅ / baseline 987/987 ✅ / test-modules 46/46 ✅ / test-data-share 46/46 ✅ / e2e 70/70 ✅。T3 因缺安卓工具链按约定跳过。请按《手工验证清单-体验问题修复-20260918.md》逐条真机/浏览器复验。

---

## 8. 补丁记录（2026-09-20）：FR-3 导览关闭方式写键（赖老师裁决）

**裁决**：所有关闭路径一律写入 `iproute.v2.guide`，重开不再弹出。

**改前关闭路径清单（实际代码核对，与任务前提有出入）**：× 按钮（template.html:673）、点遮罩（template.html:668）、跳过（template.html:677）、完成（guideNext 末步，state.js:149-151）四路**本就已**经收敛到唯一写键函数 `guideDone()`（state.js:133-136）；**唯一真实缺口是 ESC**——boot.js 原 keydown 处理器只调 `closeParams()`，对导览无任何响应（导览不关闭、不写键）。

**改动（diff 摘要）**：
- `src/app/boot.js`（+3/-1）：ESC 处理器扩展为 `closeParams()` 后判定 `#guide-sheet` 是否 open，是则调 `guideDone()`——ESC 成为第 5 条关闭路径，写键语义与其余四路完全一致（同一函数、同键名、不新增存储结构）；带 vm 桩防御守卫。
- `tests/e2e.mjs`（+约 120 行）：新增 X3-03（×）、X3-04（ESC，含"导览未开时 ESC 不得误写键"负向断言）、X3-05（遮罩）三条用例，覆盖关闭后写键 + 刷新不弹。

**门禁（2026-09-20 补丁后）**：build ✅（priceVersion `5f5f316cff2c0bb4` 不变）/ baseline-check **987/987** ✅ / test-modules **46/46** ✅ / e2e **73/73** ✅（跳过 3 个既有未实现用例：RQ-05/RQ-06/RQ-306，与上一批相同，不计入通过率）。补丁后 priceVersion/dataHash（`312542903d5ab56e`）不变，三份产物 diff 仅 builtAt/BUILD_TIME；git 仅新增 `src/app/boot.js`、`tests/e2e.mjs` 两个已跟踪文件改动，无 commit/push。

### 8.1 两处既有用例适配的来龙去脉（如实披露）

**① X2-01（FR-2 正向分支 mock 断言）竞态修复**：改前对**瞬时过渡态**「正在验证瓦片…」做时序断言（必须先轮询到过渡态、再轮询到终态绿字）。Playwright 轮询间隔下，mock Image 的 `setTimeout(0)` onload 可能先于首次轮询把文案直接推进到终态——**假阴性竞态**，与 ESC 补丁无关（前一批通过属侥幸）。改后首断言放宽为"过渡态或终态"其一，**终态绿字与探针瓦片 URL 两条硬断言一字未动**，验收强度不减。

**② E-02（无效密钥失败反馈）WAF 适配——原属已裁决维持现状项，因外部行为变化被迫适配**：E-02 的期望翻转在 2026-09-18 批次已按 FR-2/D-2 裁决定稿（无效密钥→瓦片探针常驻红字 + 不强制降级，替代旧"降级清单"期望），属**已关闭事项**。2026-09-20 复跑时失败：天地图服务端行为漂移——api 端点对乱码 tk 的响应由 HTTP 200（下发 SDK 脚本）变为 **418 且被 Chromium ORB 拦截脚本**，失败路径从"瓦片探针分支"变为"API 加载失败分支"（正确行为：降级清单 + 服务端拦截红字）。处置仅限测试断言：终态红字接受两种合法分支（瓦片被拒 / 服务端拦截脚本），并按分支双向断言降级行为（探针分支必须不降级；SDK 被拦分支必须降级），**"不得出现加载成功/密钥有效绿字假阳性"核心断言逐字保留**。产品代码零改动（PRD §9 证据边界早已声明 CloudWAF 行为不受控；真机侧亦观察到同源环境致假阴性（2026-09-18，见《FR-2勘误》））。

**过程异常记录**：e2e 前 `%TEMP%\node20portable` 被系统清理，按原做法重下 Node 20.18.1 portable 后恢复；该目录属临时环境，不入库。

---

## 9. 移植记录（2026-09-21）

**赖老师裁决（2026-09-21 16:04）**：「走移植，提交后走 PR，不带 --force，FR-4 搬完确认在最新 main 上接得上；某项上游已覆盖就不搬、记入执行报告。」

### 9.1 基线与备份

- 新克隆 `F:\projects\26-09-21\interprovince-route-optimizer`，main HEAD = `0dd06c75217a7479c39c724d00cbc216b3ef3d80`（上游自本批基线 8466155 推进 50 提交，已独立实现 FR-1~5 全部产品代码与 UX-01~09 用例）。
- 旧目录（`F:\projects\26-09-15-2`）全程只读未动；本批全部本地改动留有备份《体验修复批-本地改动备份-20260921.patch》（658,573 B），与本报告、FR-2 勘误的仓库外备份（两份 `-备份.md`）合计三份，移植前逐份核验存在且非空。

### 9.2 移植判定表（逐项比对结论：仅两份文档搬，其余均不搬）

| 项 | 判定 | 理由 |
|---|---|---|
| 《FR-2勘误-手机浏览器红字假阴性-20260920.md》 | 搬 | 文档类，随本提交入库 |
| 本执行报告（含 §8 补丁记录） | 搬 | 文档类，§9 为本次移植记录 |
| FR-4 产品代码（ai.js 失效块 + 重新推荐按钮） | 不搬 | 上游 ai.js:149 stale 判定、ai.js:172 失效块同语义同文案，renderAI 挂载点未重构，接改均不需要 |
| E-02 WAF 适配（两合法红字分支 + 按分支双向降级断言） | 不搬 | 上游 9232530 未覆盖该语义；但上游 D4 自动降级推翻 D-2，本适配「探针分支不降级」断言前提已不存在，原样搬必红 |
| X2-01 竞态修复（过渡态或终态断言） | 不搬 | 上游无此用例；tkMsgGen 代际戳（map.js:121/994/1026）在机制层消除竞态前提，断言放宽无的放矢 |
| X3-03/04/05 导览关闭写键用例 | 不搬 | 上游 #guide-box 非阻塞浮卡无 ×/遮罩/ESC 关闭路径，三条用例无定位点、断言与上游设计矛盾 |
| FR-1/2/3/5 产品代码（format/calc/map/state/boot/config/template/lib） | 不搬 | 上游均已覆盖：sanNum/fmtCompact/NUM_LIMITS、clampRegister 红框红字、tdTileProbe+clearTk+空输入保留、guideStart 写键、容量电价分区 |
| X1/X4/X5 用例、test-modules 第六节单测、RQ-401 适配、runTest 导览键预置 | 不搬 | 上游 UX-01~09 与 test-modules 第六节已等价覆盖；断言口径（clamp-bad/clamp-hint、UX 编号）全面不同，不可原样复用 |
| 构建产物三件套（index.html、shared/app-data*.json） | 不搬 | 新 main 自行构建，priceVersion 相同（5f5f316cff2c0bb4） |

### 9.3 如实记录（三则）

1. **D-2 与上游 D4 为相反裁决**：本批 PRD 决策 D-2「瓦片探针失败不强制降级（保留地图容器与叠加物）」；上游 `src/app/ui/map.js:1006-1011` `tdDegrade` 在探针失败时自动切回内置拓扑图并持久化，注释明写「grid-map-device-fixes D4，推翻《PRD-体验问题修复-20260918》决策 D-2」（真机结论：留一张没有底图的线条图比自动降级更困惑）。本批基于 D-2 的 E-02 适配、X2-02 断言因此不可移植；如需维持 D-2，须在上游另行重建该决策。
2. **9-20「ESC/遮罩/× 关闭写键」裁决在上游无落点**：该裁决针对本批模态 sheet 导览（实际代码核对后唯一缺口是 ESC，补丁收敛至 guideDone 同一写入口）。上游导览为 #guide-box 非阻塞浮卡（验收 AC3 不阻塞），关闭路径仅「跳过导览」「完成」两按钮，ESC 处理器只关参数面板与外观面板（boot.js:62），对导览无响应。是否仍要求 ESC/遮罩/× 关闭写键，待赖老师确认。
3. **新 main 原样门禁非全绿（不属本批，未修复）**：0dd06c7 零代码改动实测 e2e **89/90**，UX-04（FR-2 空输入保留密钥）在原样代码上失败——期望「已保留本机已存密钥」，实得「✗ 天地图 API 加载失败，正在区分原因…」（疑似在途加载诊断竞态或本机出网环境所致），按纪律如实记录、未做修复。其余门禁全绿：build（priceVersion 不变）/ test-modules 82/82 / test-data-share 46/46 / baseline-check 987/987（425 省对）/ model-audit 69,209 项 / regional 163,985 项 / prefill 26/26 / interaction 194/194；e2e 另跳过 3 条历史未实现用例（RQ-05/06/306）。
