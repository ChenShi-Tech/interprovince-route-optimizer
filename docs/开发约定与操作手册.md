# 开发约定与操作手册

> 面向接手本项目的人（含未来的 AI 会话）。这里记录的是**不看代码就会踩坑**的东西。
> 最后更新：2026-09-16。

---

## 一、项目是什么

省间电力现货交易的**路径优选测算工具**。给定送端省与受端省，枚举跨省输电通道的所有可行组合，按三种口径比选并给出费用拆解。

- Web 版：`index.html`，**自包含单文件**，离线可用，已发布到 `https://interprovince-route.app.workbuddy.host/`
- 安卓端：`android/` WebView 壳工程，`node android/build-apk.mjs` 出 APK
- iOS 端：`ios/`，**截至 2026-09-15 只有目录与路线图文档，Xcode 工程尚未创建**
- 数据层：`data/fixed-prices.json`（价格） + `src/extra.json`（非价格），构建后产出 `shared/app-data.json` 供两端共用

---

## 二、四条纪律（最高优先级）

### 1. 不得编造费率数值

找不到就写 `null` 并在文档里记录已检索路径。这条高于一切——一个编造的费率比一个缺失的费率危害大得多。

**同样适用于容量**：ATC（可用输电能力）我国不公开，宁可标「未获取」也不编系数。

### 2. 价格数据只有一处来源

`data/fixed-prices.json`。改价格**只改这个文件**，然后 `node tools/build.mjs`。

`docs/tariff.json` 是采集档案（含原文摘录），不参与构建。`shared/`、`index.html` 都是构建产物，**不要手改**——下次构建会被覆盖。

### 3. 费率必须分档标注来源

字段 `tier`：`gov`（发改委核定，有文号）/ `grid`（国网披露，含报备价）/ `region`（区域电网或送出省口径）。

**不得把报备价与核定价混为一谈。** 路径含非 `gov` 段时界面要主动告警。

### 4. 改动算法后必须跑基线

```
node tools/baseline-check.mjs      # 必须 693/693 通过
```

改了费率或通道数据则先 `node tools/baseline2.mjs` 重新生成基线。

---

## 三、日常开发流程

```
改代码 → node tools/release.mjs "提交信息"
```

`release.mjs` 依次做：**构建 → 5 组测试 → 提交 → 推送**，任一测试不过就中止，不会提交半成品。

5 组测试：

| 测试 | 内容 |
|---|---|
| `test-modules.mjs` | 模块结构 + 算法层纯度（不引用界面全局） |
| `test-data-share.mjs` | Web 与安卓端数据一致性 |
| `baseline-check.mjs` | **算法回归基线 693 条路线** |
| `test-prefill.mjs` | 受端参数预填行为 |
| `test-interaction.mjs` | 交互与计价口径回归 |

---

## 四、目录结构

```
src/
  template.html        页面骨架 + 样式 + 两处注入占位符
  app/
    config.js          构建期常量与本地存储键
    format.js          显示格式化与徽标
    data.js            数据装载、访问器、交给算法层的数据包
    state.js           应用状态与本地持久化（UI 层专用）
    algo/              ★ 算法层——纯函数，可被安卓/iOS 端原样复用
      network.js         图与邻接表、几何与绕行度
      cost.js            区域电网费、近似边权、单条路径精确计价
      paths.js           简单路径枚举
      solve.js           求解编排（唯一入口）
    ui/                界面层
    boot.js            启动（唯一含顶层执行语句，必须最后内联）
data/fixed-prices.json ★ 所有价格数据的唯一来源
src/extra.json         非价格数据：站点、断面、经纬度
shared/app-data.json   ★ 端云共用数据（Web 与手机端同源同版本）
docs/原始文件/         一手来源原件（发改委通知与附件 PDF）
tools/                 构建、发版、测试、归档脚本
android/  ios/  tests/
```

---

## 五、算法层为什么是纯函数

`src/app/algo/*` **不引用任何界面全局**。数据经 `data` 参数、用户参数经 `input` 传入：

```js
solve(input, data)                                 // 唯一入口
regionRate(env, code) / tariffOf(e, from) / sendFeeOf(e, from)   // 区域电价与方向相关取值
enumPaths(adj, src, dst, maxHops, cap, weightOf)   // 展开顺序由调用方给权重函数
```

**这是为了手机端能原样复用同一份算法**，不必从 HTML 里抠代码。`tools/test-modules.mjs` 会守住这条——它会检测算法层是否出现裸引用界面全局，并已用反向注入验证过守卫本身有效。

---

## 六、六个容易踩的坑

### ① 网损是乘法项，不能当边权

多段串联时送达系数 `D = Π(1−ηᵢ)`。必须两步：先用线性近似求候选路径，再用乘性公式精确重算排序。

### ② 路径标识必须用节点序列

用 `nodes.join('>')`，**不能用边对象的 `from`/`to`**——反向通行的联络线，其存储方向与行进方向相反，用边拼键会把 A→B→C 与 C→B→A 误判为同一条。

**同类错误还会出现在任何用到边方向的地方。** 凡是需要判断方向的逻辑（联络线输电价 `tariffOf`、区域电网费 `regionFee`、绕行度起点 `sideOf`），一律按实际行进的起止节点取值。

**通道方向必须逐条核对，不能一刀切。** 2026-09-15 当天走过两次弯路：先是 `buildAdj` 把每条通道都挂成双向，锦苏这类单向送电直流也能反着走（295 个连通省对里 112 个只靠反向直流连通）；随后又把全部专项工程改成单向，把德宝、青藏、长南荆这些公开报道证实双向互济的通道也砍了。现在方向由 `data/fixed-prices.json` 的「双向」字段逐条给定并附「方向依据」（S21～S27），反向时联络线取 `tRev`、双向工程取 `sendFeeRev`。

**费用口径以《省间电力现货交易规则》(2026-04，S14) 为准。** 4.3.1 的折算公式决定了所有段的输电费按段后电量计；3.3.2 决定了含网损的段不再另收网损；3.4.2(a) 决定了买方所在区域的区域电网电量电价一律计入。之前依据 1227 号第五条推断「专项工程段不计区域电网费」是错的——那是 2018 年的购电价格构成，2026 年规则对经营主体购电已明确「统一计入」。推导与原文见 `docs/05-省间现货规则核对与费用口径修正.md`，通俗讲解见 `docs/06-外送电费计算讲解.md`。

**联络线清单要对得上联网格局。** 全国是六大区域异步互联（S20），西北与华北、川渝与华中东四省、东北与华北、云南与南网主网之间都没有交流联络，只有直流或背靠背。此前的 24 条 500kV 联络线里有 6 条物理上不存在，已删除；渝鄂改记为背靠背直流。

### ③ 输出目录不能用 `dist/`

发布工具会把 `dist/` 当作构建产物排除，站点上会 404。本项目用 `shared/`。

### ④ 构建占位符必须独占一行

`src/template.html` 里 `/*__DATA__*/` 和 `/*__APP__*/` 前后不能有残留文字——替换后会变成悬空代码导致语法错误。

### ⑤ 842 号附件的单位是「元/千千瓦时」

数值上**等于 元/兆瓦时**，不要误按「分/千瓦时」再乘 10。

### ⑥ 部署是叠加式的

重新发布**不会删除**站点上已移除的文件。仓库清理后线上可能仍有残留（无害，但要知道原因）。

---

## 七、推送机制

**2026-09-16 实测：`git push` / `git pull` 已恢复可用**（代理策略放宽，当日 `git pull` 快进与 `git push origin main` 快进推送均成功），常规推送直接用 git。

`tools/push-github.mjs`（GitHub Git Data API）**保留作兜底**：代理再次挡住 `github.com` 时使用。

**它用 `base_tree` 在远端现有树上叠加本地文件**，而不是用本地文件重建整棵树——后者会在多会话并行时删掉别人推送的内容（2026-09-14 真实发生过，误删 18 个文件）。另注意：API 推送会把本地多个提交压成远端**单个提交**（取 `git log -1` 信息），历史粒度不如 git push。

两条使用注意（兜底脚本仍适用）：

- **改文件名 = 新增 + 遗留旧文件。** `base_tree` 会保留远端旧名文件形成重复。确认新旧文件 blob sha 相同后，用 `--allow-delete` 清理。
- 推送前会列出「远端有、本地没有」的文件，默认保留；确实要删须显式加 `--allow-delete`。

另：`git ls-files` 默认对中文路径做八进制转义，脚本读文件会 ENOENT，**必须用 `git -c core.quotepath=false ls-files`**。

---

## 八、发布

- 线上：`https://interprovince-route.app.workbuddy.host/`
- 管理：WorkBuddy「设置—数据管理—应用」
- 部署是本机目录的**快照**，不是 git 钩子——改完要重新发布才生效
- 部署偶发报「3000 端口未就绪」（旧静态服务占着端口），**直接重试一次即可**

---

## 九、当前状态与待办

### 已验证
- 回归基线 **693/693**
- 费率数据：30 个省输配电价、70 条通道，全部有来源文号
- 一手原件：`docs/原始文件/` S01～S28，可离线核对
- 两端数据同源同版本（`priceVersion` + `dataHash`）

### 已知缺口（不要假装它们不存在）

| 项 | 状态 |
|---|---|
| ATC（可用输电能力） | 27 条主要通道**全部未获取**，公开渠道无口径。当前容量校验用「实际输送能力」，缺失的 8 条按额定算并标注偏乐观 |
| 「省内重要输电通道」清单与限额 | 未获取（由省调披露，非交易中心）。两端省内段只能按省网输配电价近似 |
| 交易路径集合 | **确认不存在公开清单**，路径由国调中心在等值交易网络上运行时生成。已改用规则条款推导约束 |
| 西藏政府性基金及附加 | 未获取，应用内显式告警 |
| 青豫、扎青、宝合、云广、高肇、甘浙 的实际输送能力 | 未获取 |

### 尚未实现的想法（见 `docs/改进计划.md`）
时间维度（96 点时序）、两部制容量电费独立测算、中长期与现货分离、交易路径集合约束、方案并排对比。

---

## 十、数据与法规的核对依据

费率不是估的。每一条都有文号，原件在 `docs/原始文件/`：

- **省级输配电价** → 发改价格〔2026〕1077号 附件1（34 张表）
- **通道专项工程输电价** → 多个文号，存量部分主依据〔2019〕842号 附件
- **送出省输电价格** → 各省第四监管周期通知
- **定价规则** → 发改价格规〔2025〕1490号 附件4（现行）
- **购电价格构成与线损偏差归属** → 发改价格〔2018〕1227号

**定价规则谱系**：`〔2017〕2269号` → `〔2021〕1455号` → **`〔2025〕1490号`（现行）**。引用条款时注意用现行版本——1455号 已被 1490号 废止。
