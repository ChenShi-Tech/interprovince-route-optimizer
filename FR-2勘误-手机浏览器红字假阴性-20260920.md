# FR-2 勘误：手机浏览器红字假阴性（2026-09-20）

| 项 | 内容 |
|---|---|
| 关联 | PRD §4 FR-2（天地图密钥真实校验）、§9 证据边界声明 |
| 结论属性 | **环境问题勘误，非产品缺陷**；产品行为与设计一致 |

## 1. 现象

在手机浏览器（微信内打开）使用**有效密钥**时，界面出现常驻红字「✗ 瓦片请求被拒（密钥无效或被风控拦截）。」，底图不可用——与"密钥有效"事实不符，构成**假阴性**。

## 2. 成因

1. 微信内打开的页面以 `content://` 方式承载，请求天地图瓦片端点时**不带 Referer 头**；
2. 天地图 CloudWAF 将"无 Referer 的瓦片请求"判为异常来源，命中**风控冷却**（继续重试还会延长封禁，产品侧对此已有 10 分钟冷却保护逻辑）；
3. 瓦片端点拒绝 → `new Image()` 探针 onerror → 按 FR-2 设计给出红字三态定性中的"被拒"文案。

即：红字在此场景下是**风控拦截的真实反映**，密钥本身有效；判定链路（探针 onerror → 红字）按 PRD FR-2 机制正确工作，不误报"加载成功"。

## 3. 复验结论（APK 真机，2026-09-18 晚）

- 同一有效密钥输入 **APK（file:// WebView，9-18 构建的 app-debug.apk）** → 过渡态「SDK 已加载，正在验证瓦片…」→ **绿字「✓ 密钥有效，底图可用。」**，**真实瓦片正常渲染**。
- 结论：红字系上述手机浏览器环境（无 Referer + 风控冷却）所致，**非产品 bug**；FR-2 瓦片级真校验的正向分支在真机环境验证成立（补 H8 证据边界：此前该分支仅经 mock Image 断言）。

## 4. 附：负向 / 清除两态真机结论（同一轮真机复验（2026-09-18 晚））

| 态 | 操作 | 真机结果 |
|---|---|---|
| 负向（错误密钥） | 输入无效 tk 应用 | 常驻红字「✗ 瓦片请求被拒（密钥无效或被风控拦截）。」；**地图容器与线路叠加物保留、不拆图**（D-2：探针失败不强制降级）；不出现"加载成功"假阳性绿字 |
| 清除 | 点「清除密钥」 | 立即降级回内置拓扑图空态；**杀进程重开不复活**（存储已清空，`iproute.v2.map` 中密钥为空） |

## 5. 影响与建议

- 手机浏览器/微信内验收天地图时，**无 Referer 可能触发风控导致有效密钥被判红字**；以 APK 真机（或带正常 Referer 的浏览器页面）为最终验收口径。
- 产品提示为 10 分钟冷却，考虑风控封禁可能随重试延长，建议静置 30 分钟以上再试。
- 同源事项：e2e E-02 用例在 2026-09-20 因天地图服务端行为变化（api 端点对乱码 tk 由 200 变为 418 + ORB 拦截 SDK 脚本）做过一次期望适配，两者均为外部服务行为漂移，详见《执行报告-体验问题修复-20260918.md》§8。
