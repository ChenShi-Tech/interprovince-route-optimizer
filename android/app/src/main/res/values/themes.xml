<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- 应用主题（浅色系统）：状态栏 / 导航栏取清晰浅色的页头色（= src/tokens.css 的 system-bar 令牌），深色图标。
         WebView（targetSdk 34）的 prefers-color-scheme 跟随本主题的 isLightTheme：浅色系统下网页按浅色渲染，
         系统切到深色时改用 values-night/themes.xml。页面加载后，由网页通过 IPRouteShell.setSystemBars 按实际主题
         （含 app 内选的「科技」风格）再同步一次颜色。
         导航栏图标深浅的主题属性 windowLightNavigationBar 从 API 27 才有，见 values-v27；API 26 的主题里写不了，
         所以这里不设导航栏颜色，保持系统默认深色，免得白底配白色图标看不见。代码开关 SYSTEM_UI_FLAG_LIGHT_NAVIGATION_BAR
         其实 API 26 就有，MainActivity 仍按 27 门控、与这里保持一致（保守做法）；代价是 API 26 从「科技」切回「清晰」后
         导航栏停在深色，属已知现象。 -->
    <style name="AppTheme" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">#FFFFFF</item>
        <item name="android:windowLightStatusBar">true</item>
        <!-- 窗口底色 = 清晰浅色的页面底色（bg 令牌）。WebView 背景透明，网页首帧前露出的就是它，所以只在
             app 内的明暗也是浅色时不闪；app 内选了科技 / 深色时，由 MainActivity.restoreSystemBars() 在 setContentView
             之前换成网页上次在浅色系统下同步的颜色（首次安装还没有记录，这一次仍会先露出浅色）。
             系统的启动预览窗（进程起来前系统画的那一帧）只认这里，代码改不了。 -->
        <item name="android:windowBackground">@color/page_bg</item>
    </style>
</resources>
