# APP 体验问题与优化建议清单-20260917

**呈报对象**：赖老师
**测试人**：张栋俊
**测试日期**：2026-09-17
**被测版本**：Android APK，versionCode 6 / versionName 1.1.4；价格数据版本 818c4593d4b15f51（构建时间 2026-09-17 05:45，通道 64 条 / 断面 10 个）
**测试方式**：按交接文档 35 项清单逐项实测，用户视角全流程，截图留证

**严重度说明**：S1 阻断｜S2 功能错误｜S3 体验瑕疵｜S4 优化建议；标"存疑"者现象属实但归因待开发确认。

**总体结论**：35 项全部执行完毕，无 S1 阻断项。三项重点修复（底栏遮挡、天地图瓦片、容量电费测算器）均验证有效；容量电费手算核对与程序输出完全一致。新发现问题集中在：**数值输入无校验导致破版（两处）**、**天地图密钥状态误报**、**新手引导缺失**。

---

## 一、功能类

| # | 页面/模块 | 操作步骤 | 实际现象 | 期望 | 严重度 |
|---|-----------|----------|----------|------|--------|
| F1 | 全局/启动 | 连续 10 次关闭后重开 App | 平均约 2 秒白屏后正常显示；其中 1 次白屏长达约 17 秒，原因不明 | 启动加载稳定可预期，或有启动画面/进度提示 | **S2（存疑）** 疑 WebView 冷启动/资源加载，建议开发侧排查 |
| F2 | 网架图-天地图 | ①先填入正确密钥应用成功，随后删去密钥再点"应用密钥"；②填入乱码字符串作密钥点"应用密钥" | ①删去密钥后点应用，天地图依然保持加载生效（仅闪过一行"尚未填入密钥"的红字提醒，转瞬即逝），疑"空输入=沿用本机已存密钥"，是否符合设计预期待确认（大概率不符合）；②乱码密钥也显示绿字"密钥已应用，天地图加载成功｜瓦片自检 https 通道传输正常"，但实际瓦片为灰底+裂图，并未加载成功 | 应用密钥时真实校验有效性；空输入应明示"沿用已存密钥"或拒绝生效；无效/空密钥给出常驻错误提示，不显示成功 | **S2（存疑）** 自检结论与事实相反，属假阳性，会误导用户以为密钥有效 |
| F3 | 费率库 | 在费率库三个分区（通道/省级参数/断面）查找"两部制容量需量电价"表 | 无该分区；29 省分电压档容/需量单价仅能在测算页容量电费测算器内间接看到，无法在费率库独立查验 | 费率库增设该分区，便于核对各省各档单价数据来源 | **S3（存疑）** 若系刻意设计（表只服务测算器）可降为 S4 |
| F4 | 测算页-智能推荐 | 跑出推荐结果后修改测算参数（如受端省份） | 旧推荐结果直接消失，无任何提示 | 结果消失方向正确，但应在原位留一行灰字说明"参数已变化，请重新推荐"，避免用户误以为功能异常 | S4 |

## 二、界面类

| # | 页面/模块 | 操作步骤 | 实际现象 | 期望 | 严重度 |
|---|-----------|----------|----------|------|--------|
| U1 | 全局/横屏 | 切换横屏浏览 | 内容仍按竖屏窄宽居中，两侧大面积留白浪费 | 横屏合理利用宽度 | S3 |
| U2 | 测算页-价格与口径 | 送端出清价等输入框打入超长数字（如 3208434546433446…） | 主结果区破版：落地成本卡片、费用拆解条、送端净收益均显示 20 位以上天文数字并溢出卡片；无输入长度/范围限制 | 限制输入长度与合理范围，超出时提示并拒绝或兜底显示 | S3 |
| U3 | 测算页-容量电费测算器 | 容量/电量输入超大数 | 年容量电费长数字把"度电分摊"卡推出屏幕右侧，布局破版；无校验提示（正面：0 值兜底显示"—"并有说明文案） | 同上，输入校验+超长数字缩略显示 | S3（与 U2 同源：全局缺数值输入校验） |
| U4 | 网架图-天地图 | 缩放视图后查看 | 无换流站名称标注（拓扑图有）；缩放后瓦片加载需等待 | 补站点标注或说明设计意图 | S3（存疑） 待确认是层级设计还是缺失 |
| U5 | 网架图-天地图 | 拖动地图 | 拖动过程中路线消失，松手后重新绘制 | 拖动中保留路线（性能取舍可接受，建议评估） | S3 |

## 三、文案类

| # | 页面/模块 | 操作步骤 | 实际现象 | 期望 | 严重度 |
|---|-----------|----------|----------|------|--------|
| C1 | 测算页-通道组件筛选 | 点选组件过滤候选路线 | 组件颜色仅区分交直流（淡蓝=直流、灰/橄榄=交流、深蓝=选中），无"是否命中当前候选"的视觉态；测试初期曾因此误读为"筛选出错" | 增加图例或"命中/未命中"视觉区分 | S4 |
| C2 | 测算页-候选卡片 | 查看 2 段路线卡片 | 卡片只显示首段通道名，点选第二段组件筛选命中后，无法从卡片自证为什么被留下 | 卡片显示全部途经段或标注"含选中组件" | S4 |
| — | （正面记录） | — | 智能推荐断网提示（解释 CORS 原因）、未填 Key 常驻红条、天地图降级说明、费率库顶部数据来源说明、西藏"未获取"红字等文案均清楚得体 | — | 通过项 |

## 四、建议类（优化建议汇总）

| # | 建议 | 来源 | 严重度 |
|---|------|------|--------|
| J1 | **全局数值输入校验**：统一限制长度与合理范围，超长数字缩略显示（万/亿单位或科学计数），一处修复可覆盖 U2/U3 两处破版 | 4.6 / 8.4 | S4（强烈建议优先） |
| J2 | **新手引导/功能导览**：首次启动给出功能导览或"帮助"入口；容量电费测算器等实用功能默认折起、夹在大卡片之间极易被忽略，首次使用者"肯定会有不少东西找不到在哪" | 8.5 | S4（强烈建议优先） |
| J3 | 拓扑图支持点选高亮路线切换方案（目前只能看不能点） | 3.3 衍生 | S4 |
| J4 | 容量电费测算器内单价表可展开查看本省全部电压档 | 6.2 衍生 | S4 |
| J5 | 密钥应用逻辑：真实校验+空输入明示"沿用本机已存密钥"或拒绝（同 F2） | 5.6 / 8.4 | 随 F2 修复 |

## 五、已知限制（非问题，记录在案）

- 腾讯地图在 APK 内不可用属已声明降级，页面提示清楚（仅本机预览可用）。
- 天地图需用户自填 tk 密钥、仅存本机，属设计约定；断网/飞行模式下天地图与智能推荐不可用属预期，降级提示体面。

## 六、三条整体印象

1. **最值得保留的优点**：数据与计算的"可信感"。离线内置数据完整，测算三口径切换、逐段溯源、文号引用、priceVersion 版本标识一应俱全；容量电费测算器手算核对分毫不差；各处降级与空态提示写得体面清楚。这是一个让人敢用其结论的工具。
2. **最影响体验的问题**：数值输入无校验导致的破版（U2/U3）。它出现在主流程结果区，一次误触长数字就把核心结果卡片、费用条全部打乱，且无任何提示阻拦；其次是天地图密钥的"假阳性"绿字（F2），会让用户在密钥无效时误以为配置成功。
3. **只做一件事改什么**：补新手引导与功能入口可见性。功能本身已经够多够实用，但容量电费测算器、费率库筛选、组件过滤等藏在折叠卡片和不显眼的角落里，第一次用的人不看说明很难独立完成一次完整测算——加一个引导，比再加一个功能更值。

---

*本清单基于 2026-09-17 实测，全部问题均可按表中步骤复现；标"存疑"项建议开发侧确认归因后再定级。*
