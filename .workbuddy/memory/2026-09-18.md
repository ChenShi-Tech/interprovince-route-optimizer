# 2026-09-18

## openspec apply：grid-map-device-fixes（真机反馈四项修复）
- 执行 `openspec/changes/grid-map-device-fixes` 的 apply（tasks 1.1/2.1–2.4/3.1/4.1/4.2/5.1 完成；4.3 归档时回写文档、5.2 真机复验待用户装机）。
- 改动（均未提交，按用户要求只留本地，提交/推送待用户指示）：
  - `src/template.html`：徽章「公开价格 · 含历史与估算参数」→「公开数据」；帮助按钮改 ？ 36px 圆钮；`#map-view.topo-svg` 加 `touch-action:none; user-select:none`。
  - `src/app/format.js`：新增拓扑视图纯函数 `topoViewClamp/topoViewZoomAt/topoViewPan`（缩放范围 [聚焦 0.5 倍, 全图]）。
  - `src/app/state.js`：`state.mapView`（会话内触摸视野）与 `state.tdDegradeNote`（降级红字说明），saveLast 剥离。
  - `src/app/ui/calc.js`：`solveState()` 与 `pick()` 清除 `mapView`（选中方案/参数变化复位）。
  - `src/app/ui/map.js`：pointer 手势（单指平移/双指捏合/双击复位/滚轮，6px 阈值吞 click）；`topoSVG` 有 mapView 时不应用聚焦并写 `topoViewMeta`；`mapQSearch('')` 清 `mapFocusLL` 回路线视野；`loadTianditu` 密钥门槛前置 + `drawMapTD` 守卫；`tdTileProbe` onerror 自动降级（`tdDegrade`，推翻 PRD D-2）、探针成功清降级说明。
- 测试：test-modules 新增第八组视图纯函数单测（77 项全过）；e2e 更新 UX-05、新增 MF-01~07（84/84 通过，3 skip 不计入）；baseline 987/987；test-data-share 46 项全过；`node tools/build.mjs` 重建（priceVersion 5f5f316cff2c0bb4 不变）。
- 教训：e2e 页头单行断言应比「垂直中心对齐 + 无溢出」而不是 top/bottom 相等（三元素高度不同但居中同一行）。
